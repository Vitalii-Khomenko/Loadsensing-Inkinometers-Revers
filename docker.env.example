# Copy this file to .env only when the defaults do not match the Linux host.
# Obtain the values with: id -u; id -g; getent group dialout
TIL90_UID=1000
TIL90_GID=1000
TIL90_DIALOUT_GID=20
TIL90_HTTP_PORT=8765

